package com.acme.kampo.platform.inventory.domain.model.queries;

/**
 * Query to retrieve all Inventory items in the system.
 * Carries no parameters — returns the full collection.
 */
public record GetAllInventoryQuery() {}